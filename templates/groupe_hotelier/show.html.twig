{% extends 'base.html.twig' %}

{% block title %}GroupeHotelier{% endblock %}

{% block body %}
    <h1>GroupeHotelier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe_hotelier.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ groupe_hotelier.Nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ groupe_hotelier.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_groupe_hotelier_index') }}">back to list</a>

    <a href="{{ path('app_groupe_hotelier_edit', {'id': groupe_hotelier.id}) }}">edit</a>

    {{ include('groupe_hotelier/_delete_form.html.twig') }}
{% endblock %}
