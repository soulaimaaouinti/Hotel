{% extends 'base.html.twig' %}

{% block title %}GroupeHotelier index{% endblock %}

{% block body %}
    <h1>GroupeHotelier index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for groupe_hotelier in groupe_hoteliers %}
            <tr>
                <td>{{ groupe_hotelier.id }}</td>
                <td>{{ groupe_hotelier.Nom }}</td>
                <td>{{ groupe_hotelier.Description }}</td>
                <td>
                    <a href="{{ path('app_groupe_hotelier_show', {'id': groupe_hotelier.id}) }}">show</a>
                    <a href="{{ path('app_groupe_hotelier_edit', {'id': groupe_hotelier.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_groupe_hotelier_new') }}">Create new</a>
{% endblock %}
